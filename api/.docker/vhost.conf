<VirtualHost *:80>
    DocumentRoot /srv/api/public
    HttpProtocolOptions unsafe

    <Directory "/srv/api/public">
        AllowOverride all
        Require all granted
    </Directory>

</VirtualHost>

ServerName todos_api
